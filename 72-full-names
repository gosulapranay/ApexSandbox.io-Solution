public List<String> fullNames(List<String> firstNames, List<String> lastNames) {
    List<String> myList = new List<String>();
    for(Integer i = 0 ; i < firstNames.size() ; i++){
        myList.add(firstNames[i] + ' ' + lastNames[i]);
    }
    return myList;
}